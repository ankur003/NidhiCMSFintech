/*
 * TransactionInquiry Messages
 *
 * This contains all the text for the TransactionInquiry container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransactionInquiry';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionInquiry container!',
  },
});
