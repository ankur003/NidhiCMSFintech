/*
 * TransactionReport Messages
 *
 * This contains all the text for the TransactionReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TransactionReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionReport container!',
  },
});
