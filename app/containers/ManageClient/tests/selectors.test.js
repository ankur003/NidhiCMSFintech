// import { selectManageClientDomain } from '../selectors';

describe('selectManageClientDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
