/*
 * AddPrivileges Messages
 *
 * This contains all the text for the AddPrivileges container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddPrivileges';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddPrivileges container!',
  },
});
