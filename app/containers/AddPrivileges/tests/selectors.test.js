// import { selectAddPrivilegesDomain } from '../selectors';

describe('selectAddPrivilegesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
