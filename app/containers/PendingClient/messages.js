/*
 * PendingClient Messages
 *
 * This contains all the text for the PendingClient container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PendingClient';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PendingClient container!',
  },
});
