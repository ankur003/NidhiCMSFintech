// import { selectPendingClientDomain } from '../selectors';

describe('selectPendingClientDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
