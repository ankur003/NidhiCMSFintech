/*
 * AccountStatement Messages
 *
 * This contains all the text for the AccountStatement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AccountStatement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AccountStatement container!',
  },
});
