// import { selectAccountStatementDomain } from '../selectors';

describe('selectAccountStatementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
