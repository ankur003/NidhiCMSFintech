/*
 * UserVerification Messages
 *
 * This contains all the text for the UserVerification container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserVerification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserVerification container!',
  },
});
