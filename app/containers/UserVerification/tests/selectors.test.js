// import { selectUserVerificationDomain } from '../selectors';

describe('selectUserVerificationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
