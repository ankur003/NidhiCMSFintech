// import { selectWebiteLandingPageDomain } from '../selectors';

describe('selectWebiteLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
