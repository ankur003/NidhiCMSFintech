/*
 * WebiteLandingPage Messages
 *
 * This contains all the text for the WebiteLandingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WebiteLandingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WebiteLandingPage container!',
  },
});
